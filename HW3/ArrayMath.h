#pragma once
#include <iostream>

namespace ArrayMath {
	void minEvenElement(int* arr, size_t arraySize);

	uint32_t getMinAndMaxSumNuber(int* arr, size_t arraySize);

	int getOddElementsProduct(int* arr, size_t arraySize);

	void simpleSwap(int* arr, int i, int j);

	void smartSwap(int* arr, int i, int j);
}
